# Note: AI configurations (Gemini/OpenAI API keys, Ollama endpoints) are configured
# directly in the web UI under Settings > LLM Server Library and saved in the database.
# There is no need to define GOOGLE_API_KEY or GEMINI_MODEL in this .env file.

# Security Configurations
JWT_SECRET_KEY=your_secure_random_jwt_secret_key_minimum_32_chars
ENCRYPTION_KEY=your_fernet_encryption_key_base64_format

# Database configuration (Optional: Defaults to SQLite pulse.db if omitted)
# DATABASE_URL=postgresql://user:password@localhost:5432/pulse_db

# Server Settings
PORT=8000
HOST=127.0.0.1

# Open-Source Instance Settings
# ALLOW_PUBLIC_REGISTRATION=True  # Se True, qualquer pessoa pode se cadastrar. Se False, novos registros públicos são bloqueados.
# REQUIRE_INVITE_CODE=False       # Se True, novos cadastros são obrigados a fornecer um código de convite.
ALLOW_PUBLIC_REGISTRATION=True
REQUIRE_INVITE_CODE=False

# X/Twitter OAuth 2.0 Credentials (opcional: podem ser configuradas pela interface em Admin)
# App deve ser configurado como 'Web App, Automated App or Bot' com permissões 'Read and write'
# Callback URLs recomendadas no X Developer Portal:
#   http://127.0.0.1:8000/api/social/twitter/callback
#   http://localhost:8000/api/social/twitter/callback
# TWITTER_CLIENT_ID=your_twitter_client_id
# TWITTER_CLIENT_SECRET=your_twitter_client_secret
# TWITTER_REDIRECT_URI=http://127.0.0.1:8000/api/social/twitter/callback

# Meta Threads OAuth 2.0 Credentials (opcional: podem ser configuradas pela interface em Admin)
# App criado no Meta for Developers com caso de uso 'Threads API'
# Permissões: threads_basic, threads_content_publish
# Callback URLs recomendadas:
#   http://127.0.0.1:8000/api/social/threads/callback
#   http://localhost:8000/api/social/threads/callback
# THREADS_APP_ID=your_threads_app_id
# THREADS_APP_SECRET=your_threads_app_secret
# THREADS_REDIRECT_URI=http://127.0.0.1:8000/api/social/threads/callback


