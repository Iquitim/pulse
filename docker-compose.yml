version: '3.8'

services:
  pulse:
    build: .
    container_name: pulse-app
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./pulse.db:/app/pulse.db
      - ./posts:/app/posts
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
      - PORT=8000
